# progress

_The diary. Dated, appended, never edited after the fact._

## 2026-07-31

HQ stood up from the Bright & Found kit: folders, vault, core files, git initialized,
vault-health.sh passing. Checklist item 1 struck after the sweep. Pushed to the private
GitHub remote brightandfound-knowledge.

Wired and verified the Obsidian connection. The Local REST API plugin (v5.1.0) authenticates
and returns the real vault over the API, observed live. An obsidian http MCP server is set in
the HQ .mcp.json with the key inlined, because macOS GUI apps do not inherit shell env vars.
The MCP tool layer loads in a session opened in this HQ folder and is not yet exercised.
Store linking and checklist item 2 remain for the next session.

## 2026-08-16

Verified the Shopify connection reaches the real store (Bright&Found, brightandfounds.myshopify.com,
Basic plan) via get-shop-info. Recorded decision 6: the HQ runs this existing flame-diffuser store
rather than restarting. Reconciled the vault to reality: STATUS, MEMORY, README, and IMPLEMENTED no
longer claim "no store yet." Checklist left unchanged, because the store exists but has not cleared
the kit's quality bars (no verified samples, no claims sweep, no real reviews), so nothing is struck.
Node still not installed, so the toolchain (item 2) stays open.
